{
  "buildCommand": "cd frontend && npm install && NODE_OPTIONS='--max-old-space-size=4096' npm run build",
  "outputDirectory": "frontend/dist",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}