# Core dependencies (required)
fastapi==0.104.1
uvicorn[standard]==0.24.0
python-multipart==0.0.6
python-dotenv==1.0.0
openai==1.51.0
pydantic[email]==2.5.0
httpx==0.27.0

# Document processing (required for resume features)
PyPDF2
python-docx
reportlab
pdfplumber
requests
jinja2

# Authentication (required)
python-jose[cryptography]
passlib[bcrypt]

# Database (required)
sqlalchemy

# Utilities (required)
chardet
pillow

# Video processing (required for CV API - uses ffmpeg + OpenAI Vision)
# Note: opencv-python removed to save memory, using ffmpeg only
numpy

# OPTIONAL HEAVY DEPENDENCIES - Comment out to reduce memory usage:
# Uncomment these ONLY if you need local processing (not recommended for Render free tier)

# Local Whisper (NOT NEEDED - use /transcribe_api with OpenAI API instead)
# openai-whisper
# soundfile
# blis

# Emotion recognition (NOT NEEDED - basic professionalism scoring works without it)
# deepface
# tf-keras

# NLP (NOT USED)
# spacy

# OCR (NOT CRITICAL)
# pytesseract

# Note: With these commented out, memory usage drops from ~2.5GB to ~300MB
# All features still work using OpenAI APIs instead of local models
